/**
 * class containing constant values corresponding to different configuration operations.
 */
export class ConfigurationConstant {
  /**
   * default port value where the application will run if no other port value provided.
   */
  public static readonly DEFAULT_APPLICATION_SERVER_PORT: string = "8000";
}
